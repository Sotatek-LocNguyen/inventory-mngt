{
  "name": "inventory-app",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:api": "bun --cwd apps/api dev",
    "dev:web": "bun --cwd apps/web dev",
    "typecheck": "bun --cwd apps/api typecheck && bun --cwd apps/web typecheck",
    "lint": "bun --cwd apps/api lint && bun --cwd apps/web lint"
  }
}
